import 'package:flutter/material.dart';

class AppColor {
  /// Main branding color (Deep Blue/Purple from screenshot)
  static const Color primary = Color(0xFF1A0F6E);

  /// Accent Blue used in badge
  static const Color accentBlue = Color(0xFF0F8AFF);

  /// Text colors
  static const Color textDark = Color(0xFF1D1D1D);
  static const Color textLight = Color(0xFF7A7A7A);

  /// Secondary backgrounds, highlights etc.
  static const Color secondary = Color(0xFF634FEF);
  static const Color white = Colors.white;
}
